onboarding = false
# ~/.config/herdr/config.toml

[keys]
prefix           = "ctrl+space"
next_tab         = "prefix+ctrl+n"
previous_tab     = "prefix+ctrl+p"
new_tab          = ["prefix+c", "prefix+ctrl+c"]   # bind c と bind C-c の両対応
split_vertical   = ["prefix+|", "prefix+v"]   # 左右（tmux の bind | ／ v の両対応）
split_horizontal = "prefix+minus"  # 上下（tmux の bind - と一致・herdr 既定と同じ）

[ui.toast]
delivery      = "terminal"
delay_seconds = 0
